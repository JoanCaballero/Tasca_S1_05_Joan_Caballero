Executar el nivell 1, exercici 1
cd n1exercici1
mvn compile
mvn exec:java -D"exec.args"=(El path del directori que vols llistar)

Una vegada executat el exercici anterior escriu
cd ..
per ubicar-te una altre vegada al projecte general.

Executar el nivell 1, exercici 2
cd n1exercici2
mvn compile
mvn exec:java -D"exec.args"=(El path del directori que vols llistar)

Una vegada executat el exercici anterior escriu
cd ..
per ubicar-te una altre vegada al projecte general.

Executar el nivell 1, exercici 3
cd n1exercici3
mvn compile
mvn exec:java -D"exec.args"=(El path del directori que vols llistar)

Una vegada executat el exercici anterior escriu
cd ..
per ubicar-te una altre vegada al projecte general.

Executar el nivell 1, exercici 4
cd n1exercici4
mvn compile
mvn exec:java -D"exec.args"="src/main/resources/directoris.txt"